# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Settings & validation
pydantic==2.10.4
pydantic-settings==2.7.0

# Database
sqlalchemy==2.0.36
aiosqlite==0.20.0          # SQLite async driver (dev default)
asyncpg==0.30.0            # Postgres async driver (Docker / prod)
greenlet>=3.0.0            # required by SQLAlchemy async internals

# LLM providers
anthropic==0.40.0
httpx==0.28.1              # Ollama + async HTTP

# RAG / retrieval
rank-bm25==0.2.2
PyYAML==6.0.2              # frontmatter parse
markdown-it-py==3.0.0      # markdown → html server-side

# Security / sanitization
bleach==6.2.0

# Observability
structlog==24.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1              # also used in TestClient

# Misc
python-dotenv==1.0.1
